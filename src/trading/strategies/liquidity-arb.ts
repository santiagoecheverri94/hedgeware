/**
 * Algo Notes:
 * 0. (not sure if this is needed) Always keep track of last ask price?
 * 1. Check what is your current ask price
 * 2. Check if you have the positions correctly sized given current ask price
 * 3. If yes, do nothing; if no, buy position to correctly size
 * 4. Repeat for bid price (sell of step 3)
 */

// import {IBKRClient} from '../brokerage-clients/IBKR/ibkr-client'

export function startLiquidityArb(): void {
  // const brokerageClient = new IBKRClient()
}
